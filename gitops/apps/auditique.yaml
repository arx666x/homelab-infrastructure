apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: auditique
  namespace: argocd
  labels:
    app.kubernetes.io/name: auditique
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  sources:
    - repoURL: git@git.reckeweg.io:achim/homelab-infrastructure.git
      targetRevision: HEAD
      path: gitops/config/auditique

  destination:
    server: https://kubernetes.default.svc
    namespace: auditique

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
