apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - blueprints-configmap.yaml
  - deployment.yaml
  - service.yaml
  - certificate.yaml
  - ingress.yaml
  - middleware.yaml
  - outpost-passthrough-ingressroute.yaml
  - sealed-secret.yaml
  # postgres-setup.sql: kein k8s-Manifest, einmalig manuell gegen
  # gitea-postgresql ausgefuehrt (siehe Kommentar in der Datei)
