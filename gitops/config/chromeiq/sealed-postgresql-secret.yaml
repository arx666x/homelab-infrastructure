# =============================================================================
# PLATZHALTER - dieses SealedSecret ist noch NICHT generiert.
#
# Erzeugen (siehe gitops/sealed-secrets/seal-all-secrets.sh, Abschnitt "ChromeIQ"):
#   cd <repo-root>
#   ./gitops/sealed-secrets/seal-all-secrets.sh
#   -> fragt "password" (chromeiq-DB-User) und "postgres-password" (superuser)
#      interaktiv ab (maskiert) und schreibt das echte SealedSecret hierher.
#
# Solange diese Datei nur aus Kommentaren besteht, NICHT in
# kustomization.yaml resources: aufnehmen - erst nach dem echten Sealen.
#
# Referenz: chromeiq-postgresql-secret, Namespace chromeiq
#   Keys: password, postgres-password
# =============================================================================
