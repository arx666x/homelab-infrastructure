# =============================================================================
# PLATZHALTER - dieses SealedSecret ist noch NICHT generiert.
#
# Erzeugen (siehe gitops/sealed-secrets/seal-all-secrets.sh, Abschnitt
# "ArgoCD Image Updater"):
#   cd <repo-root>
#   ./gitops/sealed-secrets/seal-all-secrets.sh
#   -> fragt Gitea-Benutzername und ein Registry-Token (Scope "package:read")
#      interaktiv ab (Token maskiert) und schreibt das echte SealedSecret
#      hierher.
#
# Solange diese Datei nur aus Kommentaren besteht, NICHT in
# kustomization.yaml resources: aufnehmen - erst nach dem echten Sealen.
#
# Referenz: gitea-registry-creds, Namespace argocd (docker-registry Secret)
# =============================================================================
